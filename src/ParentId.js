export default class ParentId {
  courseId;
  groupId;

  constructor(courseId, groupId) {
    // TODO: add validation and encapsulate members
    this.courseId = courseId;
    this.groupId = groupId;
  }
}
